#pragma once
#include <QPoint>
const float HEX_SIZE = 20.f; // Distance to each corner
const auto WIDTH = sqrt(3.f) * HEX_SIZE;
const auto HEIGHT = 2.f * HEX_SIZE;

const auto HALF_WIDTH = WIDTH / 2.f;
const auto HALF_HEIGHT = HEIGHT / 2.f;